# Road Quality Analyzer - Workspace Dependencies
#
# Installs the analyzer package from ./analyzer in editable mode, which pulls in
# its runtime dependencies (numpy, pandas, scipy, matplotlib, scienceplots, folium)
# declared in analyzer/pyproject.toml. Version pins live there — not here.
#
#   pip install -r requirements.txt

-e ./analyzer

# Testing (development)
pytest>=7.0.0
